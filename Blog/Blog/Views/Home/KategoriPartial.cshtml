@model IEnumerable<Blog.Models.Kategori>

@{
    Layout = null;

}

<div class="row">
    <div class="col-lg-6">
    @foreach (var item in Model)
    {
        <ul class="list-unstyled mb-0">
            <li>
               
                @Html.ActionLink(@item.KategoriAdı.ToString(),"KategoriMakale","Home")
            </li>
        </ul>
    }
       
    </div>
    </div>




    @*<p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.KategoriAdı)
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
            {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.KategoriAdı)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.KategoriId }) |
                    @Html.ActionLink("Details", "Details", new { id = item.KategoriId }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.KategoriId })
                </td>
            </tr>
        }

    </table>*@
